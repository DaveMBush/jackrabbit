describe('SubscriptionReducer', () => {
    describe('When I change the value of name', () => {
        beforeEach(() => {

        });
        it('email should stay the same', () => {

        });
    });
});
